

const OutLineButton = ({outLineBtn}) => {
    return (
        <div>
            <button className='btn btn-outline border-2 border-[#FF3811] text-[#FF3811] my-4 flex mx-auto'>{outLineBtn}</button>
        </div>
    );
};

export default OutLineButton;